package templates

import "github.com/arunim-io/erp/templates/layouts"

templ IndexPage(usersCount int) {
	@templates.RootLayout() {
		<main>
			<h1 class="text-4xl text-blue-500">Hello, World!</h1>
			<h2>No of users: { usersCount }</h2>
		</main>
	}
}
